// Autor: Oscar Mario Alvarez Cruz

export default class Equipo {
    constructor({ id, codigo, descripcion, imagen, estado }) {
        this.id = id;
        this.codigo = codigo;
        this.descripcion = descripcion;
        this.imagen = imagen;
        this.estado = estado;
    }
}
